---
"better-convex": minor
---

Add a default `resend-email` scaffold template for `better-convex add resend` that generates `convex/functions/plugins/email.tsx` and emits explicit React Email dependency hints in human and JSON output.
