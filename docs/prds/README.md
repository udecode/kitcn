# PRDs

PRDs define one coherent kitcn capability and its local task packets. Create
them with `to-prd`, name them `<date>-<slug>.md`, and link them from the relevant
milestone map or `docs/README.md`.

They settle public API, runtime/auth/data flow, generated ownership, proof, and
non-goals before implementation. They do not replace active goal plans.
