# Milestones

Milestone maps describe a pivotable outcome and ranked PRD ladder. Create them
with `to-milestone` and name them `<date>-<slug>.md`.

Maps are planning context, not release promises. Each names the next recommended
PRD and the evidence that could change the order.
