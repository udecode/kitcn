# PR 464 465 autoclosure

Objective:
Autoclose PRs #464 and #465 as separate lanes; done when each exact PR either
passes per-PR task compliance and the full feedback/proof/merge contract, or is
commented and closed with verified receipts for missing task evidence.

Flow mode:
one-shot execution

Goal plan:
docs/plans/2026-09-14-pr-464-465-autoclosure.md

Template:
docs/plans/templates/autoclosure.md

Primary template:
docs/plans/templates/autoclosure.md

Applied packs:
- agent-native (docs/plans/templates/packs/agent-native.md)

Linked plans:
- [PR #464 task](docs/plans/2026-09-10-fix-crpc-client-error-guards.md)
- [PR #465 task](docs/plans/2026-09-11-fix-auth-jwt-cookie-hook.md)
- [Prerequisite PR #467 task](docs/plans/2026-09-14-fix-next-fixture-eslint-10-compatibility.md)

Completion threshold:
- Both PRs have immutable-head task evidence verdicts. Every compliant PR has
  zero unresolved actionable P1-or-higher feedback, exact-head terminal receipt,
  passing required checks, and final GitHub merge/closeout. Every noncompliant
  PR has one verified remediation comment and verified `CLOSED` state.
- No new product scope. Completion requires every applicable lane below to have
  fresh evidence, `bun check` passing, review findings closed, authorized
  GitHub delivery complete, and the goal checker passing.

Verification surface:
- `gh pr view`, fetched `refs/pr/<number>`, exact plan-at-head reads, full
  feedback inventories for compliant PRs, owning tests/checks, terminal receipt
  read-back, and final PR state/check read-back.

Constraints:
- Finish the intended delta; do not invent the next feature.
- Preserve source/generated/package/docs ownership.
- Use a different diagnostic after repeated failure signatures.

Boundaries:
- intended delta: existing PR #464 then #465 only, each under its own task plan
- allowed repairs: only defects required to close the existing PR contract
- unrelated files: preserve; do not treat as blockers
- non-goals: new product scope, combining task ownership, unrelated cleanup

Output budget strategy:
- Read exact PR metadata and fetched-head files first; save feedback inventories
  to `/tmp`, inspect counts and bounded slices, and cap all command output.

Blocked condition:
- Stop only for failed required comment/read-back, unavailable feedback API,
  missing merge authority, required user deferral, or a repeated external
  environment blocker after distinct repair attempts.

Start Gates:
| Gate | Applies | Evidence |
| --- | --- | --- |
| Dedicated task invocation and plan for exact PR | yes | #464 and #465 each name one distinct task plan |
| Task evidence verified at PR head | yes | fetched refs match live OIDs; both plans exist and identify their exact PR |
| Active source/plan reconstructed | yes | #464 owns cross-entrypoint cRPC guards; #465 owns the Better Auth JWT-cookie after-hook |
| Intended delta and exclusions recorded | yes | exact boundaries and non-goals are in each linked per-PR plan |
| Closure matrix classified | yes | package behavior applies; generated/docs/UI/agent-workflow lanes are N/A |
| Live PR feedback target resolved | yes | exact compliant PRs #464 and #465 |
| Feedback proof checkout bound to PR head | yes | detached local `HEAD` matched each fetched/live OID during its audit |
| Unfiltered feedback inventory | yes | helper plus raw comments/reviews and GraphQL all-thread inventory fetched for both PRs |
| GitHub delivery expectation recorded | yes | fully close feedback/checks then merge each compliant PR in order |
| Active goal checked or created | yes | created for this two-lane closeout with this plan |
| Agent-native pack selected | yes | mandated by autoclosure template |
| Agent-facing action surface identified | yes | autoclosure task compliance, feedback receipt, and merge actions |
| Source rule versus generated mirror boundary identified | yes | `.agents/rules/autoclosure.mdc` owns generated `.agents/skills/autoclosure/SKILL.md`; no edit expected |
| Installed-skill lock versus local-rule owner identified | no | N/A: no installed-skill mutation is authorized or expected |
| `agent-native-reviewer` loaded or waiver recorded | yes | loaded; N/A because neither PR changes an agent/user-action workflow surface |

Closure matrix:
| Lane | Applies | Owner/proof | Status |
| --- | --- | --- | --- |
| per-PR task ownership | yes | #464 and #465 each have an exact dedicated task plan at head | passed |
| noncompliant close | no | both task-evidence gates passed | N/A |
| source behavior | yes | focused cRPC and auth compatibility proofs | passed |
| package/API/build | yes | package build and source/dependency contract audit | passed |
| generated output | no | neither diff changes generated output or its owner | N/A |
| fixtures/scenarios | no | no fixture/scaffold behavior changed; shared fixture gate is an external baseline blocker | N/A to intended delta |
| docs/package skill | no | internal task plans only; no public docs or published skill change | N/A |
| changeset | yes | one `kitcn` patch changeset in each PR | passed |
| agent workflow | no | no agent action or workflow changed | N/A |
| live PR feedback | yes | complete inventories; zero actionable P1-or-higher items | passed |
| cleanup/review | yes | zero slop delta; P0/P1 autoreview clean on both PRs | passed |
| repository check | yes | `bun check` and both exact-head CI runs fail in generated Next fixture lint, matching current `main` | blocked |
| GitHub delivery | yes | both approved and open; merge forbidden while required CI is red | blocked |

Prerequisite lane — PR #467:
- Exact task evidence: body names
  `docs/plans/2026-09-14-fix-next-fixture-eslint-10-compatibility.md`; the plan
  identifies exactly PR #467 and exists at the pushed head.
- Ubuntu CI `34904489302` passed the full gate at the plan-bound head.
- Live review P1 `discussion_r4010211795` and P2
  `discussion_r4010211806` were accepted and repaired with red/green coverage.
- Second-pass P1 `discussion_r4010256721` was accepted: existing Next adoption
  now reconciles the lockfile/install graph after ESLint normalization even
  when no baseline package name is missing.
- Post-repair `bun check` and P0/P1 autoreview are clean. The first two threads
  have proof replies and verified resolution; final material push, the second
  P1 reply/resolution, exact-head CI, terminal receipt, and merge remain.

Work Checklist:
- [x] Every PR has its own `task` invocation and dedicated task plan; a batch
      plan or aggregate autoclosure is not used as a substitute.
- [x] Task evidence was verified from the PR body, fetched head, and exact PR
      ownership; otherwise the required comment and `CLOSED` state were read
      back and no source review, repair, merge, or release work continued.
- [x] Intended behavior and exclusions are reconstructed from real sources.
- [ ] Each lane is proven or N/A with a concrete reason.
- [x] Generated output was changed through its owner and regenerated. N/A: no
      generated owner or output changed.
- [x] Package/docs/skill/fixture/scenario/changeset contracts are synchronized.
- [ ] Full `resolve-pr-feedback` ran for the exact compliant PR; every
      actionable P1-or-higher finding was fixed, proved, replied to, and
      resolved or received the required top-level reply receipt.
- [x] For a compliant PR, local committed `HEAD`, fetched PR ref, and live
      `headRefOid` matched before proof/reply/resolution and after every push.
      For a noncompliant PR, this and all feedback gates are N/A with the
      required remediation-comment and `CLOSED` receipts.
- [x] Unfiltered top-level PR comments and review bodies were fetched through
      the GitHub API, compared by ID/URL with helper output, and every excluded
      bot/author item was ledgered; identity alone never dismissed feedback.
      Only the exact terminal receipt produced/read back by this run is exempt
      from the versioned ledger.
- [x] All inline review threads were fetched with GraphQL cursor pagination
      without filtering resolved/outdated items; every thread has priority,
      rationale, relocation, and proof state in the ledger.
- [x] Every actionable feedback item has a persisted P0-P3 priority and
      one-sentence rationale from the autoclosure rubric; ambiguous P1-versus-
      lower items fail closed as P1.
- [ ] Every P1-or-higher proof reran after the final material branch push,
      regardless of file type, including resolved or outdated threads that
      disappear from the helper's unresolved-thread output.
- [ ] Feedback was re-fetched after the last push/reply/resolution and shows
      zero unresolved actionable P1-or-higher findings.
- [ ] After all versioned plan/source updates were pushed, the exact-head P1
      proof/read-back receipt was posted to the PR and read back; no terminal
      receipt-only branch push was created. A post-comment `headRefOid` fetch
      matches the OID recorded in that receipt, and a post-comment helper/raw
      feedback fetch still shows zero actionable P1-or-higher items and no new
      URL lacking a verdict or explicit deferral, except the verified receipt.
- [x] Any remaining P2-or-lower item has its exact URL plus the user's explicit
      priority deferral recorded; no feedback was silently ignored.
- [x] Accepted cleanup and review findings are closed.
- [ ] PR body and check state match the final evidence.
- [x] Residual blocker/waiver has exact evidence and next owner.
- [x] Agent-native pack: source-of-truth rule files are edited instead of generated skill mirrors. N/A: no workflow edit.
- [x] Agent-native pack: the changed agent action is discoverable from the skill/rule text. N/A: no agent action changed.
- [x] Agent-native pack: generated mirrors are synced when `.agents/rules/**` changed, or N/A reason is recorded.
- [x] Agent-native pack: installed skills are changed only through
      `npx skills add/update/remove`; local rules/templates/helpers stay source-owned.
- [x] Agent-native pack: routing, required receipts, placeholder failure,
      completion representability, and forbidden behavior have eval/smoke rows.
- [x] Agent-native pack: accepted agent-native review findings are fixed or explicitly rejected with reason. N/A: reviewer skill loaded and no workflow surface applies.

Error attempts:
| Failure signature | Count | Next different move | Resolution |
| --- | ---: | --- | --- |
| `gh run view 34830750129 --log-failed` exceeded the output budget and was truncated | 1 | Read job metadata, then filter the exact job log to bounded error lines | resolved: isolated `react/display-name`/ESLint 10 failure |
| malformed `gh search prs 'repo:…'` query | 1 | pass the repository through `--repo` instead | resolved: no separate repair PR or issue found |
| goal completion checker reports open feedback/receipt/delivery gates and the not-yet-merged #465 linked plan is absent from the #464 checkout | 1 | keep the goal incomplete until prerequisite CI repair lands and both PR heads can be re-proved | expected blocker |

Completion Gates:
| Gate | Applies | Required action | Evidence |
| --- | --- | --- | --- |
| Per-PR task ownership | yes | Record exact PR and dedicated task-plan path | passed for #464 and #465 |
| Noncompliant PR disposition | no | Verify task evidence or comment then close and read back | N/A: both compliant |
| Targeted behavior proof | yes | Run smallest missing owning proof | passed for cRPC and auth lanes |
| Source/generated audit | yes | Prove correct source and regenerated mirrors | passed; no generated owner changed |
| Package/docs/scenario closure | yes | Run every applicable local contract | package lanes pass; repo fixture gate blocked upstream |
| Feedback proof checkout | yes | Compliant PR only: require local committed `HEAD` = fetched PR ref = live `headRefOid` before proof/reply/resolution and at terminal verification | passed during both audits; terminal verification pending because no merge-ready receipt may be posted |
| Live PR feedback resolution | yes | Compliant PR only: run full `resolve-pr-feedback` and close every actionable P1-or-higher finding; otherwise N/A with noncompliant stop receipts | passed: zero actionable P1-or-higher findings |
| Feedback priority classification | yes | Compliant PR only: persist P0-P3 plus rationale for every actionable item; classify ambiguous P1-versus-lower as P1 | complete in ledgers below |
| Final P1 proof replay | conditional | Compliant PR only: after the final material branch push, rerun every P1-or-higher proof, including resolved/outdated items | passed: no material push and no actionable P1 finding |
| Final live feedback read-back | conditional | Compliant PR only: re-fetch helper plus unfiltered top-level/all-thread inventories; require zero actionable P1-or-higher and explicit P2-or-lower deferrals | passed at both unchanged exact heads; no actionable P2-or-lower finding exists |
| External terminal receipt | conditional | Compliant PR only: post/read exact-head receipt; require receipt/live/fetched/local OID equality and no unrecorded helper/raw URL except that verified receipt | pending |
| Deslop | yes | Run bounded cleanup or N/A | passed: zero occurrence/score delta for each PR |
| Agent-native reviewer | no | Run for workflow changes or N/A | N/A: no agent/user-action workflow surface changed |
| Final lint | yes | Run `bun lint:fix` | source tree lint passed; no local repair to format |
| Repository check | yes | Run `bun check` | blocked by exact baseline fixture failure |
| GitHub delivery | yes | Commit/push/open or update PR and read back | both approved; merge blocked by required CI |
| Autoreview | yes | Resolve every accepted actionable finding | passed: clean P0/P1 run for each PR |
| Goal plan complete | yes | Run `node .agents/skills/autogoal/scripts/check-complete.mjs docs/plans/2026-09-14-pr-464-465-autoclosure.md` | pending |
| Agent source / generated sync | no | Run `bun install` when `.agents/rules/**` changed and verify generated mirrors | N/A: no agent source changed |
| Installed lock audit | no | Verify expected lock entries and removed skills through CLI-managed state | N/A: no installed skill changed |
| Agent action discoverability | no | Source-audit the skill/rule path an agent will read | N/A: no agent action changed |
| Helper and template smoke | no | Syntax-check helpers and prove incomplete failure/completed representation when applicable | N/A: no helper/template changed |
| Agent-native review | no | Load `.agents/skills/agent-native-reviewer/SKILL.md` and close accepted findings, or record N/A | skill loaded; review not applicable |

Phase / pass table:
| Phase | Status | Evidence | Next |
| --- | --- | --- | --- |
| Inventory | complete | immutable heads, plans, diffs, and full feedback inventories | repair |
| Repair | complete | no in-scope source repair required | review |
| Review/checks | blocked | focused proofs and reviews pass; shared fixture CI is red | maintainer fixes baseline CI |
| Delivery | blocked | both approvals verified; required CI and Vercel authorization remain red | rerun exact-head checks |
| Closeout | pending | terminal receipts and merges are forbidden until blockers clear | final audit |

Verification evidence:
- Compliance #464: live/fetched OID `ee407dc237531664ee9da3eb1c78069c2d3010eb`;
  body has one task-plan line; plan exists at that head and identifies #464.
- Compliance #465: live/fetched OID `23782b315e02af4dfb7dd84a0043a851018fa6be`;
  body has one task-plan line; plan exists at that head and identifies #465.
- #464 focused proof: package build, 36 cRPC tests, root typecheck, and root
  lint passed. `bun check` passed 1435 Bun tests, 1050 Vitest tests, 124 CLI
  tests, and Concave smoke before generated Next fixture lint failed.
- #464 structured review: P0/P1 autoreview clean at the exact PR head with
  confidence 0.95; slop delta had zero added/worsened occurrences.
- #465 focused proof: package build and source Vitest passed 2/2; the packed
  public entrypoint passed 2/2 on Better Auth 1.7.1, 1.7.3, and 1.7.4.
- #465 structured review: P0/P1 autoreview clean at the exact PR head with
  confidence 0.94; slop delta had zero added/worsened occurrences.
- Required CI is red on #464 run `34830750129`, #465 run `34833218958`, and
  current-main run `34713975670` with the same generated Next fixture error:
  ESLint 10.10.0 loads `react/display-name`, then
  `contextOrFilename.getFilename is not a function`.
- GitHub ruleset `12831593` requires the `CI` check. The current user can
  bypass it, but autoclosure forbids merging against a red required gate.
- Maintainer approvals were posted and read back without changing either head:
  #464 review `5203193839`; #465 review `5203193836`.

Feedback ledger — PR #464:
| URL | Kind | Priority | Rationale | State |
| --- | --- | --- | --- | --- |
| https://github.com/udecode/kitcn/pull/464#issuecomment-5662196958 | changeset bot | N/A | informational changeset receipt; exact head and expected `kitcn` patch are present | closed |
| https://github.com/udecode/kitcn/pull/464#issuecomment-5662197235 | Vercel bot | N/A | external-contributor preview notice; Vercel is not a required ruleset check and this package-only PR changes no rendered surface | informational |
| https://github.com/udecode/kitcn/pull/464#pullrequestreview-5203193839 | maintainer review | N/A | this run's verified approval and blocker summary | closed |

Feedback ledger — PR #465:
| URL | Kind | Priority | Rationale | State |
| --- | --- | --- | --- | --- |
| https://github.com/udecode/kitcn/pull/465#issuecomment-5662526264 | Vercel bot | N/A | external-contributor preview notice; Vercel is not a required ruleset check and this package-only PR changes no rendered surface | informational |
| https://github.com/udecode/kitcn/pull/465#issuecomment-5662526302 | changeset bot | N/A | informational changeset receipt; exact head and expected `kitcn` patch are present | closed |
| https://github.com/udecode/kitcn/pull/465#pullrequestreview-5203193836 | maintainer review | N/A | this run's verified approval and blocker summary | closed |

Inventory reconciliation:
- Helper output: one changeset comment, one maintainer review body, and zero
  inline threads per PR.
- Raw REST output: the same helper-visible items plus one Vercel bot comment
  per PR. No other top-level reviews exist.
- Raw GraphQL cursor inventory: zero resolved, unresolved, or outdated inline
  threads for both PRs; `hasNextPage=false`.
- No P2-or-lower actionable feedback exists, so user deferral is N/A.

Timeline:
- 2026-09-14T21:30:25.521Z Autoclosure plan created.
- 2026-09-14T21:41:37Z Exact-head maintainer approvals posted and read back for both PRs.

Reboot status:
| Question | Answer |
| --- | --- |
| Where am I? | Blocked at the shared required CI gate |
| Where am I going? | Rerun exact-head checks, post terminal receipts, then merge in order |
| What is the goal? | Close #464 and #465 honestly under separate task evidence and feedback receipts |
| What have I learned? | Both diffs are review-clean; current `main` and both PRs share the same required fixture failure |
| What have I done? | Verified compliance, source behavior, dependency contracts, feedback inventories, reviews, approvals, and blocker read-backs |

Open risks:
- Required `CI` cannot become green until the unrelated generated Next fixture
  ESLint 10/plugin incompatibility is repaired in its own compliant task lane.
- Vercel previews remain unavailable for these external-contributor heads, but
  Vercel is not required by the main ruleset and neither PR changes UI.
- Pushing any material update invalidates the exact-head proofs and approvals;
  rerun the full feedback and P1 proof loop after the final push.
